<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Matcha - Connection</title>
        <link href="/styles/styles.css" rel="stylesheet" type="text/css">
        <!-- Latest compiled and minified CSS -->
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
        <!-- jQuery library -->
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
        <!-- Popper JS -->
        <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
        <!-- Latest compiled JavaScript -->
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
    </head>
 
    <body>
        <div class="container-fluid">
            <h1 class="text-center">Bienvenu sur Matcha !</h1>
            <div class="d-flex justify-content-center align-items-center">
                <% if (user == 'false') { %>
                    <div class="alert alert-danger alert-dismissible fade show">
                        <button type="button" class="close" data-dismiss="alert">&times;</button>
                        Cet utilisateur n'existe pas. Vérifie ton pseudo ou inscris-toi.
                    </div>
                <% } else if (password == 'false') { %>
                    <div class="alert alert-danger alert-dismissible fade show">
                        <button type="button" class="close" data-dismiss="alert">&times;</button>
                        Ce mot de passe est erronné.
                    </div>
                <% } else if (creation == 'false') { %>
                    <div class="alert alert-success alert-dismissible fade show">
                        <button type="button" class="close" data-dismiss="alert">&times;</button>
                        Ton compte a bien été créé, tu peux désormais te connecter !
                    </div>
                <% } %>
                <div>
                    <form action="/connect" method="post">
                        <input required type="text" name="user_connect[name]" placeholder="Ton Pseudo">
                        <input required type="password" name="user_connect[mdp1]" placeholder="Ton Password">
                        <input class="btn btn-dark" type="submit" value="Se Connecter">
                    </form>
                    <p class="text-center">Clique <a class="text-danger" href="/creation">ICI</a> pour créer ton compte.</p>
                    <p class="text-center">Clique <a class="text-danger" href="/reset_password">ICI</a> si tu as oublié ton mot de passe.</p>
                </div>
            </div>
        </div>
    </body>
</html>