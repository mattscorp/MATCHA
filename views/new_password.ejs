<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Matcha - Nouveau mot de passe</title>
        <link href="/styles/styles.css" rel="stylesheet" type="text/css">
        <!-- Latest compiled and minified CSS -->
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
        <!-- jQuery library -->
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
        <!-- Popper JS -->
        <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
        <!-- Latest compiled JavaScript -->
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
    </head>
 
    <body>
        <div class="container-fluid">
            <h1>Change ton mot de passe !</h1>
            <% if (mdp_match == 'false') { %>
                <p class="alert alert-danger">Les mots de passes ne sont pas identiques.</p>
            <% } else if (mdp_strength == 'false') { %>
                <p class="alert alert-danger">Cet mot de passe n'est pas assez sécurisé : ton mot de passe doit comporter au moins 8 caractères, donc au moins un chiffre, une majuscule, une minuscule et un caractère spécial parmi ceux-ci : $@$!%*#?&.</p>
            <% } %>
            <form action="/new_password" method="post">
                <input type="hidden" name="email" value="<%= email %>">
                <input required type="password" name="mdp1" placeholder="Ton mot de passe">
                <input required type="password" name="mdp2" placeholder="Confirme ton mot de passe">
                <input type="submit" value="Valider">
            </form>
        </div>
    </body>
</html>