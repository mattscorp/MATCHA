<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>Matcha - Nouveau mot de passe</title>
        <link href="/styles/styles.css" rel="stylesheet" type="text/css">
    </head>
 
    <body>
        <h1>Change ton mot de passe !</h1>
        <% if (mdp_match == 'false') { %>
            <p class="alert_message">Les mots de passes ne sont pas identiques.</p>
        <% } else if (mdp_strength == 'false') { %>
            <p class="alert_message">Cet mot de passe n'est pas assez sécurisé : ton mot de passe doit comporter au moins 8 caractères, donc au moins un chiffre, une majuscule, une minuscule et un caractère spécial parmi ceux-ci : $@$!%*#?&.</p>
        <% } %>
        <form action="/new_password" method="post">
            <input type="hidden" name="email" value="<%= email %>">
            <input type="password" name="mdp1" placeholder="Ton mot de passe">
            <input type="password" name="mdp2" placeholder="Confirme ton mot de passe">
            <input type="submit" value="Valider">
        </form>
    </body>
</html>